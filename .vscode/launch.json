{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/hello",
            "cwd": "${workspaceRoot}",
            "osx": {
                "MIMode": "lldb"
            }
        }
    ]
}